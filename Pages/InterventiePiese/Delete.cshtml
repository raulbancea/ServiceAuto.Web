@page
@model ServiceAuto.Web.Pages.InterventiePiese.DeleteModel

<h1>Delete</h1>

<h4>Are you sure you want to delete this?</h4>

<div>
    <dl class="row">
        <dt class="col-sm-3">Cantitate</dt>
        <dd class="col-sm-9">@Model.InterventiePiesa.Cantitate</dd>

        <dt class="col-sm-3">Pret unitar</dt>
        <dd class="col-sm-9">@Model.InterventiePiesa.PretUnitarLaDataAplicarii</dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="InterventiePiesa.InterventieId" />
        <input type="hidden" asp-for="InterventiePiesa.PiesaDeSchimbId" />

        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-page="Index" class="btn btn-secondary">Back</a>
    </form>
</div>
